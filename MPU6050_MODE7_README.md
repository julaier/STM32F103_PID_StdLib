# MPU6050 模式 7 测试说明

这份说明只负责陀螺仪，不测试电机，也不测试循迹。

## 一、接线

| MPU6050 | STM32F103C8T6 |
| --- | --- |
| VCC | 3.3V |
| GND | GND |
| SCL | PB10 |
| SDA | PB11 |
| AD0 | 不接 |
| INT | 不接 |
| XDA | 不接 |
| XCL | 不接 |

`AD0` 不接时，MPU6050 的 7 位 I2C 地址是 `0x68`。代码已经按 `0x68` 配置，不需要改成 `0x69`。

PB10 是 I2C2_SCL，PB11 是 I2C2_SDA。它们和编码器、电机 PWM、循迹、USART1 都不冲突。

## 二、串口接线

| STM32 | USB 转串口模块 |
| --- | --- |
| PA9，USART1_TX | 模块 RX |
| PA10，USART1_RX | 模块 TX |
| GND | GND |

串口参数是 `115200`、8 数据位、1 停止位、无校验。

## 三、烧录后怎么测

1. 打开 `Project.uvprojx`，确认 `Example/user_code.c` 里的模式是 `7U`。
2. 编译并下载程序。
3. 上电后先不要动小车，保持静止约 1 秒。
4. 串口先出现：

```text
MPU6050 init OK. Keep the car still...
GYRO calib done bias_raw_x100=...
```

5. 然后每隔约 100 ms 输出一次：

```text
GYRO z=... rate_x100=... angle_x10=...
```

## 四、输出怎么看

- `z`：陀螺仪 Z 轴原始值。静止时应该在零偏附近小幅变化。
- `rate_x100`：Z 轴角速度，单位是度每秒，数值已经乘了 100。例如 `100` 表示 `1.00 dps`。
- `angle_x10`：角速度积分得到的角度，单位是度，数值已经乘了 10。例如 `900` 表示 `90.0 度`。

把小车平放，绕竖直方向慢慢向左和向右转动：

- 一个方向的 `rate_x100` 应该主要为正。
- 另一个方向的 `rate_x100` 应该主要为负。
- 转一圈后看 `angle_x10` 是否接近正负 `3600`。这只是快速检查，测试时会有一定漂移。

## 五、初始化失败怎么查

如果串口只输出：

```text
MPU6050 init failed...
```

按这个顺序检查：

1. 模块是不是供电了，优先用 3.3V，GND 必须和 STM32 共地。
2. SCL 是否接 PB10，SDA 是否接 PB11。
3. SDA、SCL 是否接反。
4. `AD0` 是否确实悬空。如果它被接到了 3.3V，地址会变成 `0x69`，当前代码读不到。
5. 裸芯片的 SDA、SCL 需要上拉到 3.3V。常见 GY-521 模块一般自带上拉，但自制板要自己加 4.7 kOhm 上拉。
6. 检查 PB10、PB11 到模块的焊点或杜邦线，轻轻动一下线看是否接触不良。
